import React from 'react';

const Landing = () => {
    return (
        <div style={{ textAlign: 'center' }}>
            <h1>
                Survey Provider
            </h1>
            Collec feedback from your users
        </div>
    );
};

export default Landing;